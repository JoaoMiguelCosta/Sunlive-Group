import { FruitBasketIcon, WaterIcon } from "../../shared/ui/icons/index.js";

/**
 * Exclusivos Hotel (1 brand).
 * Ícones globais (2+ brands) devem ficar no shared.
 */
export const BRAND_ICONS = Object.freeze({
  FruitBasketIcon,
  WaterIcon,
});

export default BRAND_ICONS;
