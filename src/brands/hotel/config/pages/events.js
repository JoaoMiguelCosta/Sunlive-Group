import { eventsCelebrations, customizedQuotes, } from "../sections/events/index.js";

const events = {
  id: "events",
  sections: {
    eventsCelebrations,
    customizedQuotes,
  },
};

export default events;
