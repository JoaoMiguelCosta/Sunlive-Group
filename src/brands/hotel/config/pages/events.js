import customizedQuotes from "../sections/events/customizedQuotes.js";
import eventsCelebrations from "../sections/events/eventsCelebrations.js";

const events = {
  id: "events-page",
  ui: {
    pageAriaLabel: "Estalagem de Sangalhos — Events",
  },
  sections: {
    customizedQuotes,
    eventsCelebrations,
  },
};

export default events;
