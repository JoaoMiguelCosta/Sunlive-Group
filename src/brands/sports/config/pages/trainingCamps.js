import {
  hero,
  overview,
  pillars,
  programs,
  closingPanel,
} from "../sections/trainingCamps/index.js";

const trainingCamps = {
  id: "training-camps",
  sections: {
    hero,
    overview,
    pillars,
    programs,
    closingPanel,
  },
};

export default trainingCamps;
