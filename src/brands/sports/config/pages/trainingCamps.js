import { hero, programs, cta } from "../sections/trainingCamps/index.js";

const trainingCamps = {
  id: "training-camps",
  sections: {
    hero,
    programs,
    cta,
  },
};

export default trainingCamps;
