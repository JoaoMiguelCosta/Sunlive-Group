import {
  hero,
  overview,
  pillars,
  closingPanel,
} from "../sections/events/index.js";

const events = {
  id: "events",
  sections: {
    hero,
    overview,
    pillars,
    closingPanel,
  },
};

export default events;
