import { contactCTA } from "../sections/contacts/index.js";

const contacts = {
  id: "contacts",
  sections: {
    contactCTA,
  },
};

export default contacts;
export { contacts as travelContactsPage };
